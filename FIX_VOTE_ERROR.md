# แก้ไข Error: Invalid action สำหรับระบบโหวต

## 🔴 ปัญหา

เมื่อเปิด Tab "Vote" จะเห็น error:

```
Invalid action. ใช้ "check", "checkin", หรือ "history" เท่านั้น
```

**สาเหตุ:** Apps Script ที่ deploy ไปยังเป็นเวอร์ชันเก่าที่ไม่มี vote actions

---

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: อัพเดท Apps Script Code

1. เปิด Google Sheet ที่ใช้สำหรับระบบ
2. ไปที่ **Extensions** → **Apps Script**
3. **ลบโค้ดเก่าทั้งหมด** ใน editor
4. **คัดลอกโค้ดทั้งหมด** จากไฟล์ `apps-script.js` ในโปรเจคนี้
5. **วางโค้ดใหม่** ใน Apps Script Editor
6. **บันทึก** (Ctrl+S หรือ Cmd+S)

### ขั้นตอนที่ 2: Deploy ใหม่

#### วิธีที่ 1: Update Deployment เดิม (แนะนำ)

1. ใน Apps Script Editor คลิก **Deploy** → **Manage deployments**
2. คลิกไอคอน **Edit** (✏️) ที่ deployment เดิม
3. เลือก **New version**
4. คลิก **Deploy**
5. **ไม่ต้องเปลี่ยน URL** - ใช้ URL เดิมได้เลย

#### วิธีที่ 2: สร้าง Deployment ใหม่

1. ใน Apps Script Editor คลิก **Deploy** → **New deployment**
2. เลือก **Web app**
3. ตั้งค่า:
   - **Execute as**: Me
   - **Who has access**: Anyone
4. คลิก **Deploy**
5. **คัดลอก Web App URL ใหม่**
6. ไปอัพเดทในไฟล์ `.env`:
   ```
   VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_NEW_SCRIPT_ID/exec
   ```
7. **Restart dev server** (ถ้ากำลังรันอยู่)

---

## ✅ ตรวจสอบว่าแก้ไขสำเร็จ

1. เปิดเว็บแอปพลิเคชัน
2. ไปที่ Tab "Vote"
3. **ไม่ควรเห็น error** แล้ว
4. ควรเห็น "Loading vote options..." หรือตัวเลือกโหวต (ถ้ามีข้อมูลใน Sheet)

---

## 📝 หมายเหตุ

- **ไม่ต้องเปลี่ยน URL** ถ้าใช้วิธี Update Deployment เดิม
- **ต้องเปลี่ยน URL** ถ้าสร้าง Deployment ใหม่
- Apps Script จะสร้าง Tab "Vote Options" และ "Vote Results" อัตโนมัติถ้ายังไม่มี

---

## 🔍 ตรวจสอบ Logs (ถ้ายังมีปัญหา)

1. ใน Apps Script Editor ไปที่ **View** → **Logs**
2. ลองโหวตอีกครั้ง
3. ดู error messages ใน Logs
4. ตรวจสอบว่า:
   - Tab "Vote Options" มีชื่อถูกต้อง
   - มีข้อมูลใน Sheet (แถวที่ 2 เป็นต้นไป)
