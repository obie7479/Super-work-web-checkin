# คู่มือการตั้งค่า Google Sheet สำหรับระบบโหวต

คู่มือนี้จะช่วยคุณตั้งค่า Google Sheet สำหรับระบบโหวต (Voting System)

## 📋 ขั้นตอนที่ 1: สร้าง Tab "Vote Options"

### 1.1 สร้าง Tab ใหม่ใน Google Sheet

1. เปิด Google Sheet ที่ใช้สำหรับระบบ Check-in (Sheet เดียวกัน)
2. คลิกที่ **"+"** ด้านล่างเพื่อสร้าง Tab ใหม่
3. ตั้งชื่อ Tab เป็น **"Vote Options"** (ต้องตรงกับชื่อนี้ทุกตัวอักษร)

### 1.2 ตั้งค่า Header (แถวแรก)

ในแถวแรก (Row 1) ใส่ header ตามนี้:

| A            | B            | C            | D            | E            | F            | ... |
| ------------ | ------------ | ------------ | ------------ | ------------ | ------------ | --- |
| **Work/Job** | **Option 1** | **Option 2** | **Option 3** | **Option 4** | **Option 5** | ... |

**หรือพิมพ์ในเซลล์:**

- A1: `Work/Job`
- B1: `Option 1`
- C1: `Option 2`
- D1: `Option 3`
- E1: `Option 4`
- (เพิ่ม Option 5, 6, 7... ตามต้องการ)

### 1.3 ใส่ข้อมูลตัวเลือกโหวต

เริ่มจากแถวที่ 2 (Row 2) เป็นต้นไป ใส่ข้อมูลตามนี้:

**ตัวอย่าง:**

| Work/Job | Option 1   | Option 2   | Option 3   | Option 4     |
| -------- | ---------- | ---------- | ---------- | ------------ |
| โปรเจค A | ตัวเลือก 1 | ตัวเลือก 2 | ตัวเลือก 3 | ตัวเลือก 4   |
| โปรเจค B | ดีมาก      | ดี         | ปานกลาง    | ต้องปรับปรุง |
| งาน X    | ใช่        | ไม่ใช่     | -          | -            |

**คำอธิบาย:**

- **คอลัมน์ A (Work/Job)**: ชื่องาน/โปรเจคที่ต้องการให้โหวต
- **คอลัมน์ B, C, D, ... (Option 1, 2, 3, ...)**: ตัวเลือกที่ผู้ใช้สามารถโหวตได้
- **แต่ละแถว** = หนึ่งงาน พร้อมตัวเลือกโหวต
- **สามารถเพิ่มคอลัมน์ Option ได้ไม่จำกัด** (Option 5, 6, 7, ...)

### 1.4 ตัวอย่างข้อมูลจริง

```
Row 1: Work/Job | Option 1 | Option 2 | Option 3
Row 2: งานพัฒนาเว็บไซต์ | React | Vue.js | Angular
Row 3: เลือกสีธีม | สีน้ำเงิน | สีเขียว | สีแดง
Row 4: วันที่จัดงาน | 15 ม.ค. | 20 ม.ค. | 25 ม.ค.
```

---

## 📊 ขั้นตอนที่ 2: Tab "Vote Results" (สร้างอัตโนมัติ)

**ไม่ต้องสร้างเอง!** Tab "Vote Results" จะถูกสร้างอัตโนมัติเมื่อมีการโหวตครั้งแรก

**โครงสร้าง Tab "Vote Results":**

| A             | B           | C             | D            | E                   |
| ------------- | ----------- | ------------- | ------------ | ------------------- |
| **Timestamp** | **User ID** | **User Name** | **Work/Job** | **Selected Option** |

**คำอธิบาย:**

- **Timestamp**: เวลาที่โหวต
- **User ID**: ID ของผู้ใช้ที่โหวต
- **User Name**: ชื่อผู้ใช้ที่โหวต
- **Work/Job**: งานที่โหวต
- **Selected Option**: ตัวเลือกที่เลือก

---

## ⚙️ ขั้นตอนที่ 3: ตรวจสอบ Apps Script

### 3.1 ตรวจสอบว่า Apps Script ถูกตั้งค่าแล้ว

1. ไปที่ Google Sheet → **Extensions** → **Apps Script**
2. ตรวจสอบว่าโค้ดใน `apps-script.js` ถูกวางไว้แล้ว
3. ตรวจสอบว่า `SHEET_ID` ถูกตั้งค่าแล้ว

### 3.2 Deploy Apps Script (ถ้ายังไม่ได้ทำ)

1. ใน Apps Script Editor คลิก **Deploy** → **New deployment**
2. เลือก **Web app**
3. ตั้งค่า:
   - **Execute as**: Me
   - **Who has access**: Anyone
4. คลิก **Deploy**
5. คัดลอก **Web App URL** ไปใส่ในไฟล์ `.env`:
   ```
   VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
   ```

---

## ✅ ขั้นตอนที่ 4: ทดสอบระบบ

### 4.1 ตรวจสอบข้อมูลใน Sheet

1. เปิด Tab "Vote Options"
2. ตรวจสอบว่ามีข้อมูลอย่างน้อย 1 แถว (นอกจาก header)
3. ตรวจสอบว่าคอลัมน์ A (Work/Job) มีข้อมูล
4. ตรวจสอบว่ามีตัวเลือกอย่างน้อย 1 ตัวเลือกในแต่ละแถว

### 4.2 ทดสอบในเว็บ

1. เปิดเว็บแอปพลิเคชัน
2. ไปที่ Tab "Vote"
3. ควรเห็นตัวเลือกโหวตที่ตั้งค่าไว้ใน Sheet
4. ลองโหวตดู
5. ตรวจสอบ Tab "Vote Results" ใน Google Sheet ว่ามีข้อมูลถูกบันทึกหรือไม่

---

## 📝 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: โหวตเลือกเทคโนโลยี

**ใน Tab "Vote Options":**

| Work/Job        | Option 1 | Option 2 | Option 3 |
| --------------- | -------- | -------- | -------- |
| เลือก Framework | React    | Vue.js   | Angular  |

**ผลลัพธ์:** ผู้ใช้จะเห็นงาน "เลือก Framework" พร้อมตัวเลือก 3 ตัวเลือก

### ตัวอย่างที่ 2: โหวตหลายงาน

**ใน Tab "Vote Options":**

| Work/Job | Option 1   | Option 2   | Option 3 |
| -------- | ---------- | ---------- | -------- |
| งาน A    | ตัวเลือก 1 | ตัวเลือก 2 | -        |
| งาน B    | ดีมาก      | ดี         | ปานกลาง  |
| งาน C    | ใช่        | ไม่ใช่     | -        |

**ผลลัพธ์:** ผู้ใช้จะเห็น 3 งาน และสามารถโหวตได้งานละ 1 ครั้ง

---

## ⚠️ ข้อควรระวัง

1. **ชื่อ Tab ต้องตรงกันทุกตัวอักษร**: "Vote Options" (มีตัวพิมพ์ใหญ่-เล็ก)
2. **Header ต้องอยู่ในแถวแรก**: Row 1 เท่านั้น
3. **ข้อมูลเริ่มจากแถวที่ 2**: Row 2 เป็นต้นไป
4. **คอลัมน์ A (Work/Job) ต้องมีข้อมูล**: ไม่สามารถว่างได้
5. **ต้องมีตัวเลือกอย่างน้อย 1 ตัวเลือก**: ในแต่ละแถวต้องมี Option อย่างน้อย 1 คอลัมน์

---

## 🔧 แก้ไขปัญหา

### ปัญหา: ไม่เห็นตัวเลือกโหวตในเว็บ

**วิธีแก้:**

1. ตรวจสอบว่า Tab "Vote Options" มีชื่อถูกต้อง
2. ตรวจสอบว่ามีข้อมูลในแถวที่ 2 เป็นต้นไป
3. ตรวจสอบว่า Apps Script ถูก deploy แล้ว
4. ตรวจสอบ Console ใน Browser (F12) ว่ามี error หรือไม่

### ปัญหา: โหวตแล้วแต่ไม่บันทึก

**วิธีแก้:**

1. ตรวจสอบว่า Tab "Vote Results" ถูกสร้างแล้ว (จะสร้างอัตโนมัติ)
2. ตรวจสอบว่า Apps Script มี permission ที่ถูกต้อง
3. ตรวจสอบ Logs ใน Apps Script Editor

---

## 📞 ต้องการความช่วยเหลือ?

หากมีปัญหาหรือคำถามเพิ่มเติม:

1. ตรวจสอบ Logs ใน Apps Script Editor (View → Logs)
2. ตรวจสอบ Console ใน Browser (F12)
3. ดูไฟล์ `TROUBLESHOOTING.md` สำหรับปัญหาทั่วไป
